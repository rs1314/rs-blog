$(function() {
    function t(t) {
        var e = 0;
        return $(t).each(function() {
            e += $(this).outerWidth(!0)
        }),
            e
    }
    function e(e) {
        var a = t($(e).prevAll()),
            i = t($(e).nextAll()),
            n = t($(".content-tabs").children().not(".rs_menuTabs")),
            s = $(".content-tabs").outerWidth(!0) - n,
            r = 0;
        if ($(".page-tabs-content").outerWidth() < s) r = 0;
        else if (i <= s - $(e).outerWidth(!0) - $(e).next().outerWidth(!0)) {
            if (s - $(e).next().outerWidth(!0) > i) {
                r = a;
                for (var o = e; r - $(o).outerWidth() > $(".page-tabs-content").outerWidth() - s;) r -= $(o).prev().outerWidth(),
                    o = $(o).prev()
            }
        } else a > s - $(e).outerWidth(!0) - $(e).prev().outerWidth(!0) && (r = a - $(e).prev().outerWidth(!0));
        $(".page-tabs-content").animate({
                marginLeft: 0 - r + "px"
            },
            "fast")
    }
    function a() {
        var e = Math.abs(parseInt($(".page-tabs-content").css("margin-left"))),
            a = t($(".content-tabs").children().not(".rs_menuTabs")),
            i = $(".content-tabs").outerWidth(!0) - a,
            n = 0;
        if ($(".page-tabs-content").width() < i) return ! 1;
        for (var s = $(".rs_menuTab:first"), r = 0; r + $(s).outerWidth(!0) <= e;) r += $(s).outerWidth(!0),
            s = $(s).next();
        if (r = 0, t($(s).prevAll()) > i) {
            for (; r + $(s).outerWidth(!0) < i && s.length > 0;) r += $(s).outerWidth(!0),
                s = $(s).prev();
            n = t($(s).prevAll())
        }
        $(".page-tabs-content").animate({
                marginLeft: 0 - n + "px"
            },
            "fast")
    }
    function i() {
        var e = Math.abs(parseInt($(".page-tabs-content").css("margin-left"))),
            a = t($(".content-tabs").children().not(".rs_menuTabs")),
            i = $(".content-tabs").outerWidth(!0) - a,
            n = 0;
        if ($(".page-tabs-content").width() < i) return ! 1;
        for (var s = $(".rs_menuTab:first"), r = 0; r + $(s).outerWidth(!0) <= e;) r += $(s).outerWidth(!0),
            s = $(s).next();
        for (r = 0; r + $(s).outerWidth(!0) < i && s.length > 0;) r += $(s).outerWidth(!0),
            s = $(s).next();
        n = t($(s).prevAll()),
        n > 0 && $(".page-tabs-content").animate({
                marginLeft: 0 - n + "px"
            },
            "fast")
    }
    function n() {
        var t = $(this).attr("href"),
            a = $(this).data("index"),
            i = $.trim($(this).text()),
            n = !0;
        if (void 0 == t || 0 == $.trim(t).length) return ! 1;
        if ($(".rs_menuTab").each(function() {
                return $(this).data("id") == t ? ($(this).hasClass("active") || ($(this).addClass("active").siblings(".rs_menuTab").removeClass("active"), e(this), $(".rs_mainContent .rs_iframe").each(function() {
                    return $(this).data("id") == t ? ($(this).show().siblings(".rs_iframe").hide(), !1) : void 0
                })), n = !1, !1) : void 0
            }), n) {
            var s = '<a href="javascript:;" class="active rs_menuTab" data-id="' + t + '">' + i + ' <i class="fa fa-times-circle"></i></a>';
            $(".rs_menuTab").removeClass("active");
            var r = '<iframe class="rs_iframe" name="iframe' + a + '" width="100%" height="100%" src="' + t + '?v=3.0" frameborder="0" data-id="' + t + '" seamless></iframe>';
            $(".rs_mainContent").find("iframe.rs_iframe").hide().parents(".rs_mainContent").append(r);
            var o = layer.load();
            $(".rs_mainContent iframe:visible").load(function() {
                layer.close(o)
            }),
                $(".rs_menuTabs .page-tabs-content").append(s),
                e($(".rs_menuTab.active"))
        }
        return ! 1
    }
    function s() {
        var t = $(this).parents(".rs_menuTab").data("id"),
            a = $(this).parents(".rs_menuTab").width();
        if ($(this).parents(".rs_menuTab").hasClass("active")) {
            if ($(this).parents(".rs_menuTab").next(".rs_menuTab").size()) {
                var i = $(this).parents(".rs_menuTab").next(".rs_menuTab:eq(0)").data("id");
                $(this).parents(".rs_menuTab").next(".rs_menuTab:eq(0)").addClass("active"),
                    $(".rs_mainContent .rs_iframe").each(function() {
                        return $(this).data("id") == i ? ($(this).show().siblings(".rs_iframe").hide(), !1) : void 0
                    });
                var n = parseInt($(".page-tabs-content").css("margin-left"));
                0 > n && $(".page-tabs-content").animate({
                        marginLeft: n + a + "px"
                    },
                    "fast"),
                    $(this).parents(".rs_menuTab").remove(),
                    $(".rs_mainContent .rs_iframe").each(function() {
                        return $(this).data("id") == t ? ($(this).remove(), !1) : void 0
                    })
            }
            if ($(this).parents(".rs_menuTab").prev(".rs_menuTab").size()) {
                var i = $(this).parents(".rs_menuTab").prev(".rs_menuTab:last").data("id");
                $(this).parents(".rs_menuTab").prev(".rs_menuTab:last").addClass("active"),
                    $(".rs_mainContent .rs_iframe").each(function() {
                        return $(this).data("id") == i ? ($(this).show().siblings(".rs_iframe").hide(), !1) : void 0
                    }),
                    $(this).parents(".rs_menuTab").remove(),
                    $(".rs_mainContent .rs_iframe").each(function() {
                        return $(this).data("id") == t ? ($(this).remove(), !1) : void 0
                    })
            }
        } else $(this).parents(".rs_menuTab").remove(),
            $(".rs_mainContent .rs_iframe").each(function() {
                return $(this).data("id") == t ? ($(this).remove(), !1) : void 0
            }),
            e($(".rs_menuTab.active"));
        return ! 1
    }
    function r() {
        $(".page-tabs-content").children("[data-id]").not(":first").not(".active").each(function() {
            $('.rs_iframe[data-id="' + $(this).data("id") + '"]').remove(),
                $(this).remove()
        }),
            $(".page-tabs-content").css("margin-left", "0")
    }
    function o() {
        e($(".rs_menuTab.active"))
    }
    function d() {
        if (!$(this).hasClass("active")) {
            var t = $(this).data("id");
            $(".rs_mainContent .rs_iframe").each(function() {
                return $(this).data("id") == t ? ($(this).show().siblings(".rs_iframe").hide(), !1) : void 0
            }),
                $(this).addClass("active").siblings(".rs_menuTab").removeClass("active"),
                e(this)
        }
    }
    function c() {
        var t = $('.rs_iframe[data-id="' + $(this).data("id") + '"]'),
            e = t.attr("src"),
            a = layer.load();
        t.attr("src", e).load(function() {
            layer.close(a)
        })
    }
    $(".rs_menuItem").each(function(t) {
        $(this).attr("data-index") || $(this).attr("data-index", t)
    }),
        $(".rs_menuItem").on("click", n),
        $(".rs_menuTabs").on("click", ".rs_menuTab i", s),
        $(".rs_tabCloseOther").on("click", r),
        $(".rs_tabShowActive").on("click", o),
        $(".rs_menuTabs").on("click", ".rs_menuTab", d),
        $(".rs_menuTabs").on("dblclick", ".rs_menuTab", c),
        $(".rs_tabLeft").on("click", a),
        $(".rs_tabRight").on("click", i),
        $(".rs_tabCloseAll").on("click",
            function() {
                $(".page-tabs-content").children("[data-id]").not(":first").each(function() {
                    $('.rs_iframe[data-id="' + $(this).data("id") + '"]').remove(),
                        $(this).remove()
                }),
                    $(".page-tabs-content").children("[data-id]:first").each(function() {
                        $('.rs_iframe[data-id="' + $(this).data("id") + '"]').show(),
                            $(this).addClass("active")
                    }),
                    $(".page-tabs-content").css("margin-left", "0")
            })
});